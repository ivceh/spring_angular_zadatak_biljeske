spring.application.name=spring_zadatak_biljeske

spring.security.user.name=korisnik
spring.security.user.password=korisnik

spring.datasource.url=jdbc:mysql://localhost:3306/biljeske_app?useSSL=false
spring.datasource.username=root
spring.datasource.password=

#postgresql://ivceh:...

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update

logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

# Enable SQL logging
#spring.jpa.show-sql=true

# Format SQL output for readability (optional)
#spring.jpa.properties.hibernate.format_sql=true

# Log SQL parameter values (for Hibernate 5.x and higher)
#spring.jpa.properties.hibernate.type=trace

# Enable SQL logging
logging.level.org.hibernate.SQL=DEBUG

# Log bind parameters (actual values)
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE